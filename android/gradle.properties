# Force Gradle to use Java 17 (fixes JBR/Java 21 crashes)
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.16.8-hotspot

# JVM/Daemon ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â keep heap reasonable and avoid reusing a bad daemon state
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.daemon=false

# Concurrency ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â single worker, no parallel tasks (lower RAM spikes)
org.gradle.parallel=false
org.gradle.workers.max=1

# Build only 64-bit ARM to cut native compile time/RAM (debug)
reactNativeArchitectures=arm64-v8a

# AndroidX
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# New Architecture / Hermes ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â OFF for this debug build to shrink native workload
newArchEnabled=false
hermesEnabled=true

# Edge-to-edge
edgeToEdgeEnabled=true
expo.edgeToEdgeEnabled=true

# Image formats
expo.gif.enabled=true
expo.webp.enabled=true
expo.webp.animated=false

# Dev tools
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Packaging
expo.useLegacyPackaging=false
hermesEnabled=true
newArchEnabled=false
newArchEnabled=false
